[Service]
# NVIDIA Parakeet TDT 0.6B v3 int8 via ONNX Runtime (AVX2). Faster and more
# accurate than Whisper base.en on this CPU. Whisper Vulkan remains at
# /usr/lib/voxtype/voxtype-vulkan if we switch back.
ExecStart=
ExecStart=/usr/lib/voxtype/voxtype-onnx-avx2 daemon
